import numpy as np

def get_neighbors(row, col):
    """ returns array of neighbors
    [0,0,0,None]
    """

def generate_image(height, width):
    random_image = np.random.randint(2, size=(height, width))
    for row in range(height):
        for col in range(width):


if __name__ == "__main__":
    print("Hello world")
